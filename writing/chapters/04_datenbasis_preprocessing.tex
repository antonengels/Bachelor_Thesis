\section{Datengrundlage}

\subsection{Spezifikation der empirischen Datenbasis}

Den Ausgangspunkt der empirischen Untersuchung bildet die rohe, unmodifizierte Datenbasis, welche in diesem Unterkapitel in ihrer ursprünglichen Form vorgestellt wird. 
Methodisch stellt diese initiale Charakterisierung eine fundamentale Voraussetzung dar: 
Ein neuronales Netz lernt die physikalischen Grenzen und Spezifika seiner Trainingsdaten -- die sogenannte \gls{ODD} -- blind auswendig. 
Die exakte Definition der Datenherkunft ist folglich unabdingbar, um die späteren Generalisierungsgrenzen und potenziellen Schwächen des trainierten Modells fundiert einordnen zu können.

Die zugrundeliegenden Betriebsdaten stammen von Siemens Vectron-Lokomotiven (siehe Abbildung \ref{fig:vectron}), welche im Erfassungszeitraum als Alleinfahrer, das heißt ohne angekuppelten Wagenzug, operierten. 
Der für die Modellierung verwendete Arbeitsdatensatz umfasst ausschließlich verifizierte Fahrten auf der Betuweroute, einer dedizierten, flach trassierten Güterzugstrecke, wobei die Fahrten von einer Vielzahl unterschiedlicher Triebfahrzeugführer durchgeführt wurden. 
Diese operativen Randbedingungen haben tiefgreifende Auswirkungen auf die Modellbildung: Die individuellen Fahrstile der menschlichen Fahrer in identischen Streckensituationen induzieren eine erhebliche stochastische Varianz, die für das Modell als Rauschen im Datensatz in Erscheinung tritt. 
Darüber hinaus warnt das flache Streckenprofil bereits a priori vor einem signifikanten \textit{Covariate Shift}: Sollte das trainierte Modell später in der Inferenzphase auf bergigen Streckentopologien eingesetzt werden, trifft es zwangsläufig auf eine gänzlich unbekannte Zustandsverteilung.


\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\textwidth]{vectron.jpg}
    \caption[Vectron-Lokomotive]{Abgebildet ist eine der beiden Siemens-Vectron-Lokomotiven, die für die Datenerhebung eingesetzt wurden \cite{vectron}.}
    \label{fig:vectron}
\end{figure}

Der erfasste Datensatz erstreckt sich über einen kontinuierlichen Aufzeichnungszeitraum von 84 Tagen (Daten aus 2026) und umfasst ein Speichervolumen von circa 50 Gigabyte im komprimierten \texttt{pcapng.gz}-Format. 
Die Datenerfassung erfolgte über ein ethernet-basiertes Aufzeichnungssystem. 
Auf Ebene der Quellsysteme beschreiben die folgenden Werte die Nenn-Abtastraten (Soll-Spezifikation) der jeweiligen Subsysteme im \gls{CCS}-Verbund (\gls{ETCS}, \gls{RST}, \gls{ATO}): 
Aktorik-Signale mit 20\,Hz (50\,ms), kinematische Größen mit 5\,Hz (200\,ms) und statische Streckendaten mit bis zu 1\,Hz (1000\,ms). 
Diese Angaben charakterisieren die diskrete Sensor- beziehungsweise Telegrammaktualisierung auf Subsystemebene und sind von der später beobachteten Empfangsstatistik des aggregierten \gls{ORD}-Datenstroms strikt zu trennen. 

Durch die formale Strukturierung lassen sich die extrahierten Roh-Variablen kategorisieren und den Dimensionen des Zustands- und Aktionsraums zuweisen. 
Die Fahrzeugkinematik wird durch die Ist-Geschwindigkeit (\texttt{V\_EST}) und die Beschleunigung (\texttt{A\_EST}) abgebildet. Die restriktiven Streckenlimits werden über die zulässige Höchstgeschwindigkeit \texttt{V\_PERMITTED} (\gls{ETCS}-OB Paket 6) repräsentiert. 
Das Signal \texttt{A\_EST} liegt dabei im Rohdatenstrom als herstellerspezifisch skaliertes Festkomma-Signal in \(\mathrm{mm/s^2}\) vor, wobei \(1\,\text{Unit}=0{,}001\,\mathrm{m/s^2}\) gilt (\(1000\,\text{Units}=1{,}0\,\mathrm{m/s^2}\)). 
Die im Datensatz beobachteten Wertebereiche von ungefähr \([-1000,+700]\) entsprechen damit physikalisch plausiblen Verzögerungen bis etwa \(-1{,}0\,\mathrm{m/s^2}\) (Betriebsbremsung) sowie Anfahrbeschleunigungen bis etwa \(+0{,}7\,\mathrm{m/s^2}\) auf der Betuweroute. 
Im späteren Preprocessing werden diese Rohwerte mittels Z-Score-Standardisierung \(z=(x-\mu)/\sigma\) in einen zentrierten, dimensionslosen Zustandsraum für PyTorch überführt.
Aus protokolltechnischer Sicht des \gls{ATO}-over-\gls{ETCS}-Standards (insbesondere Paket 4/103) gehören dazu ferner Topologie- und Kontextfelder wie der Streckengradient (\texttt{A\_GRADIENT}) und die Schienenhaftung (\texttt{Q\_ADHESION}) sowie fahrzeuginterne Rückmeldungen (\texttt{M\_RST\_TBsetVal}). 
Die anschließende Sichtung des vorliegenden \gls{ORD}-Rohdatenextrakts zeigte jedoch, dass \texttt{Q\_ADHESION} in der verfügbaren Aufzeichnungsstufe nicht belegt war und somit als systematisch fehlendes Signal zu behandeln ist. 
Methodisch wird \texttt{Q\_ADHESION} daher als standardseitig relevantes, im konkreten Datensatz jedoch nicht beobachtbares Feld klassifiziert und folgerichtig nicht in den tatsächlich modellierten Feature-Vektor aufgenommen. 
Dem gegenüber steht die Zielvariable \texttt{M\_ATO\_RTBRq} -- die vom menschlichen Fahrer geforderte Hebelstellung. Diese wird ausdrücklich als das Label $Y$ definiert, welches es zu prädizieren gilt. 
Durch diese präzise Zuweisung von Input-Features $X$ und Label $Y$ wird das unstrukturierte, reale Zug-Szenario mathematisch formalisiert und in ein klassisches Regressionsproblem des Supervised Learning ($X \rightarrow Y$) übersetzt.

Trotz dieser methodischen Fundierung weist der Datensatz a priori signifikante physikalische Limitationen auf, die offen adressiert werden müssen. 
Es liegt ausschließlich die Dynamik einer leeren Lokomotive vor; die immense Trägheit und die komplexe Wagendynamik eines schweren Güterzuges sind in diesen Daten nicht abgebildet. 
Ferner enthält der Datensatz fundamentale \textit{Hidden Variables} (unsichtbare Ereignisse) -- wie beispielsweise den direkten Sichtkontakt des Triebfahrzeugführers zu vorausliegenden Signalen oder Hindernissen, welche nicht im System protokolliert werden. 
Damit ist das zentrale Risiko der \textit{Causal Confusion} (Ursachenverwechslung) im Imitation Learning gegeben: Das Modell beobachtet Expertenreaktionen, deren kausaler Auslöser nicht im Zustandsvektor $X$ enthalten ist, und kann die Aktion folglich nur über sichtbare, aber ursächlich unvollständige Korrelate approximieren. 
Formal entsteht eine Fehlzuordnung zwischen unbeobachteter Ursache und beobachtbarer Wirkung, wodurch das Netzwerk Scheinkorrelationen erlernt und die zugrundeliegende Fahrdynamik nur verzerrt internalisiert. 
Dieser Umstand erzeugt ein systematisches, nicht auflösbares Label-Rauschen, welches die Generalisierungsfähigkeit der Policy begrenzt und die Robustheit gegenüber ungesehenen Situationen signifikant reduziert.

\subsection{Explorative Datenanalyse und Bereinigung}

Bevor die historischen Rohdaten in formale Tensordarstellungen für das Training des neuronalen Netzes überführt werden können, ist eine systematische \gls{EDA} mit Datenbereinigung methodisch unumgänglich. 
Künstliche neuronale Netze agieren fundamental als hochdimensionale, aber blinde Mustererkenner, die jegliche in den Eingangsdaten vorhandenen Korrelationen ungefiltert auf die Zielvariablen abbilden. 
Dies unterstreicht das fundamentale Paradigma des maschinellen Lernens „Garbage In, Garbage Out“, wonach fehlerhafte, verrauschte oder unvollständige Eingangsdaten in der Konsequenz zu dysfunktionalen und nicht generalisierbaren Prädiktionsmodellen führen.

In diesem Kontext fungiert die \gls{EDA} als zentrales Diagnosewerkzeug. 
Sie dient dazu, systematisches Rauschen, Ausreißer und Anomalien in den realen Betriebsdaten frühzeitig zu identifizieren und durch geeignete Vorverarbeitungsschritte zu filtern. 
Durch diesen strengen Aufbereitungsprozess wird sichergestellt, dass das Modell im späteren Verlauf ausschließlich valide physikalische Kausalitäten der Zugsteuerung abstrahiert und nicht fälschlicherweise temporäre Sensorfehler oder Dateninkonsistenzen als gültige Fahrstrategie erlernt.

\subsubsection{Data Cleaning und zeitliche Synchronisierung}

Im ersten Schritt der Datenbereinigung müssen systemspezifische Fehlercodes, sogenannte Sentinels, sowie ungültige Messungen adressiert werden. 
Dem vorgelagert wurde ein strikter Topologie-Filter angewandt: Alle Fahrten, die nicht zweifelsfrei der Betuweroute zugeordnet werden konnten (z.\,B. Rangierfahrten, Zubringerabschnitte oder Trips mit unvollständigen \gls{GPS}-/Topologie-Logs), wurden konsequent ausgeschlossen. 
Diese Selektion ist methodisch notwendig, um eine konsistente Strecken- und Gradientenbasis (\texttt{A\_GRADIENT}) sicherzustellen und Störrauschen aus heterogenen Infrastrukturen im Zustandsraum zu vermeiden. 
Der finale Arbeitsdatensatz umfasst damit 403 Trips und besteht zu 100\,\% aus verifizierten Betuweroute-Fahrten; dies bildet die systematische Grundlage für das spätere trip-basierte Splitting. 
Das Aufzeichnungssystem protokolliert Sensorausfälle durch fest definierte Extremwerte, wie beispielsweise $65535$ bei Geschwindigkeitssignalen (\texttt{V\_EST}, \texttt{V\_PERMITTED}) und $\pm 32768$ beziehungsweise $32767$ bei Beschleunigungs- und Gradientendaten (\texttt{A\_EST}, \texttt{A\_GRADIENT}). 
Diese artifiziellen Konstanten wurden konsequent durch \gls{NaN} ersetzt. 
Ergänzend erfolgte eine Out-of-Range-Maskierung, um physikalisch unmögliche Werte zu filtern. 
So wurden glitchbedingte Geschwindigkeitsspitzen von bis zu 371\,km/h eliminiert, wodurch sich ein realistisches Maximum von 141\,km/h bei einem 99,9-Perzentil von 125\,km/h einstellte. 
Nach dieser Bereinigung ergaben sich unterschiedliche Nutzbarkeitsquoten: 
Während zentrale Signale und Labels (\texttt{V\_EST}, \texttt{M\_ATO\_RTBRq}, \texttt{M\_RST\_TBsetVal}, \texttt{M\_RST\_SlipSlide}) eine lückenlose Verfügbarkeit von 100\,\% aufwiesen, fiel diese bei \texttt{V\_PERMITTED}, \texttt{A\_EST} und \texttt{A\_GRADIENT} durch einen 11,49\,\%igen Sentinel-Ausfall auf 88,5\,\%. 
Stark ausgedünnt zeigten sich die Features \texttt{D\_STPDISTANCE} (38,2\,\% Nutzbarkeit aufgrund von 61,8\,\% fehlenden Werten) und \texttt{TOPO\_Q\_RADIUS} (35,1\,\%), was deren uneingeschränkte Nutzung limitiert.

Methodisch begründet sich die Maskierung dieser Sentinels unmittelbar aus den Anforderungen stabiler Modelloptimierung. 
Unmaskierte Extremwerte würden nachfolgende Skalierungsverfahren, wie die Min-Max- oder Z-Score-Normalisierung, vollständig dominieren, wodurch die reale Signalvarianz auf annähernd null gestaucht würde. 
Werden derart verzerrte Werte in das neuronale Netz gespeist, erzeugen sie extreme Aktivierungen im Forward-Pass. 
Dies führt deterministisch zu verschwindenden oder explodierenden Gradienten (Vanishing/Exploding Gradients) und verhindert eine stabile Konvergenz des Modells während des Trainings.

Neben der Wertebereinigung erforderte die asynchrone und uneinheitliche Aufzeichnungsfrequenz der Rohdaten eine zeitliche Synchronisierung. 
Das ursprüngliche Median-Abtastintervall betrug circa 34\,ms, wies jedoch eine starke Spanne von 8 bis 118\,ms auf. 
Dies entspricht einer effektiven Ereignisdichte von etwa 28 bis 30\,Hz im aggregierten Datenstrom des On-board Recording Devices (\gls{ORD}), nicht jedoch einer physikalischen Abtastrate einzelner Sensoren. 
Die Diskrepanz zwischen Soll-Nennfrequenzen der Quellsysteme und beobachteter Ist-Dichte entsteht durch asynchrone Telegrammankünfte, Bus-Overhead, Jitter, Paketierung und das Übertragungsverhalten des ethernet-basierten \gls{CCS} Consist Networks. 
Um aus diesem asynchronen Ist-Datenstrom einen für PyTorch konsistenten, synchronen State Space zu erzeugen, wurden die Signalgruppen mittels Zeit-Binning, Outer-Join und anschließendem Resampling mit Forward-Fill auf ein konstantes Zeitraster überführt (modellabhängig 200\,ms/5\,Hz beziehungsweise 100\,ms/10\,Hz). 
Die durch diese Diskretisierung verbleibenden Datenlücken erwiesen sich als minimal: 
Der Median der größten Lücke pro Fahrt (Trip) lag bei lediglich 173\,ms. Lücken von über 2\,s existierten mit durchschnittlich 0,05 Vorkommnissen pro Trip praktisch nicht. 
Zur Sicherstellung der zeitlichen Kohärenz wurden Trips bei Unterbrechungen von mehr als 60\,s systematisch in separate Teilsequenzen getrennt.

Die Reduktion der zeitlichen Auflösung (Downsampling) von 30\,Hz auf 5\,Hz lässt sich physikalisch und signaltheoretisch begründen. 
Die steuerungsrelevante Fahrdynamik einer Vectron-Lokomotive wird durch Aktorik- und Trägheitszeitkonstanten im Bereich von etwa $1{,}0\,\text{s}$ bis $2{,}0\,\text{s}$ (Bremsdruckaufbau/Ansprechverhalten) sowie kinematische Reaktionszeiten bis ungefähr $3{,}0\,\text{s}$ geprägt. 
Daraus folgt eine dominante Dynamik mit Grenzfrequenzen deutlich unterhalb von $1\,\text{Hz}$; hochfrequente Anteile oberhalb von etwa $5\,\text{Hz}$ (unter 200\,ms) sind im vorliegenden \gls{ORD}-Strom daher überwiegend auf Sensor-Jitter, Paketierung und Bus-Overhead zurückzuführen, nicht auf reale Fahr- oder Bremszustandsänderungen. 
Mit einer Abtastrate von $f_s=5\,\text{Hz}$ wird die Nyquist-Bedingung für die relevante Systemdynamik ($f_{max}\approx0{,}5\text{ bis }1{,}0\,\text{Hz}$, also $f_s \gg 2f_{max}$) klar erfüllt, sodass die fahrdynamisch relevanten Signalverläufe aliasing-frei und ohne informationskritischen Verlust repräsentiert werden. 
Durch das Downsampling wird die Rechenlast drastisch reduziert und die extrem hohe Autokorrelation benachbarter Frames minimiert, wodurch redundante Informationen aus dem Datensatz entfernt werden. 
Dies führt zu kompakteren Sequenzlängen, was insbesondere das \gls{BPTT}-Verfahren beim Training rekurrenter neuronaler Netze über lange zeitliche Horizonte signifikant stabilisiert.

\subsubsection{Analyse der Klassenverteilung}

Eine detaillierte Untersuchung der Werteverteilungen von Eingangsfeatures und Zielvariablen ist von zentraler Bedeutung, um potenzielle Verzerrungen (Bias) im Modelltraining zu antizipieren. 
Abbildung \ref{fig:a_est_dist} illustriert exemplarisch die Verteilung des bereinigten Beschleunigungssignals \texttt{A\_EST} über den gesamten Datensatz. 
Auf der Abszisse sind die physikalischen Schätzwerte aufgetragen, während die Ordinate die absolute Häufigkeit der Vorkommnisse auf einer logarithmischen Skala darstellt. 
Die Verteilung zeigt eine deutliche Konzentration um den Nullpunkt, was die physikalische Realität der Trägheit und langer Phasen konstanter Geschwindigkeit im Schienenverkehr widerspiegelt.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{02_distributions.png}
    \caption[Verteilung von A\_EST]{Verteilung des Features \texttt{A\_EST} nach der Bereinigung. Die x-Achse zeigt die Signalwerte, die y-Achse die absolute Häufigkeit im Datensatz (logarithmische Skalierung).}
    \label{fig:a_est_dist}
\end{figure}

Analog zur Analyse der Eingangsfeatures wurde die Zielvariable, die Hebelstellung \texttt{M\_ATO\_RTBRq}, eingehend untersucht. 
Wie im linken Teildiagramm von Abbildung \ref{fig:label_dist} dargestellt, lässt sich das Fahrverhalten hinsichtlich der Aktionsrichtung in drei grundlegende Klassen unterteilen. 
Beschleunigungsphasen (Werte $> 0$) machen mit 49,2\,\% knapp die Hälfte der Daten aus. 
Bremsvorgänge (Werte $< 0$) repräsentieren 27,8\,\% der Aufzeichnungen. 
Die verbleibenden 23,0\,\% entfallen auf die exakte Neutralstellung beziehungsweise das Rollen des Zuges (Werte $= 0$). 
Das rechte Teildiagramm verdeutlicht ergänzend die Werteskalierung der aktiven Aktionen: 
Es zeigt sich, dass sich auch die tatsächlichen Hebelbewegungen stark um kleine Beträge nahe der Neutrallage konzentrieren und extreme Auslenkungen eher selten vorkommen.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1\textwidth]{07_label_analysis.png}
    \caption[Verteilung der Zielvariable]{Analyse der Zielvariable \texttt{M\_ATO\_RTBRq}. Links: Prozentuale Verteilung der diskreten Hebelzustände (Bremsen, Neutral, Beschleunigen). Rechts: Histogramm der kontinuierlichen Label-Werte (exklusive exakter Nullwerte), welches die starke Konzentration um kleine Beträge aufzeigt.}
    \label{fig:label_dist}
\end{figure}

Für das Training des neuronalen Netzes birgt diese ausgeprägte Nullwert-Dominanz der Zielvariable ein erhebliches Risiko für den Optimierungsprozess. 
Der Anteil von 23\,\% exakt nullwertigen Labels in Kombination mit den dicht angrenzenden, kleinen Hebelstellungen impliziert eine starke Massenkonzentration bei $y=0$ (Beharrungs- beziehungsweise Rollphasen). 
Begrifflich wird dieses Muster in Anlehnung an die Zähldatenstatistik häufig als „Zero-Inflation“ bezeichnet; im vorliegenden kontinuierlichen Setting ist dies jedoch als bewusste Analogie zu verstehen. 
Statistisch liegt hier eine Mischstruktur aus einer Punktmasse bei $0$ (Dirac-artiger Anteil) und einer kontinuierlichen Dichte auf den Intervallen $[-1,0)$ sowie $(0,1]$ vor. 
Ohne geeignete Gegenmaßnahmen tendiert das Modell dadurch zu einer Regression-zum-Mittel in Richtung $0$, da eine nahezu konstante Null-Prädiktion unter Standard-\gls{MSE} einen künstlich günstigen Gesamtloss erzeugen kann, ohne die kausale Fahrdynamik belastbar zu erlernen.

Um eine rein statische Roll-Strategie zu vermeiden, muss diese Asymmetrie in der Trainingsphase explizit adressiert werden. 
Dazu zählen robuste Verlustfunktionen (z.\,B. Huber-Loss statt reinem \gls{MSE}), eine aktionsphasensensitive Evaluierung sowie balancierende Sampling-Strategien, um die Repräsentativität dynamischer Beschleunigungs- und Bremsentscheidungen im Datensatz gezielt zu erhöhen.

\subsubsection{Korrelationsanalyse}

Um die statistischen Abhängigkeiten innerhalb des bereinigten Datensatzes zu quantifizieren und die Vorhersagekraft der einzelnen Sensordaten zu evaluieren, wurde eine Korrelationsanalyse durchgeführt. 
Die physikalischen Gesetzmäßigkeiten der Zugsteuerung -- beispielsweise das Verhältnis zwischen Bremskraft, Geschwindigkeit und Anhalteweg -- sind von ausgeprägten Sättigungseffekten und Nichtlinearitäten geprägt. 
Aus diesem Grund wurde für die Untersuchung der rangbasierte Spearman-Korrelationskoeffizient ($\rho$) gewählt. 
Im Gegensatz zur klassischen Pearson-Korrelation, welche ausschließlich lineare Zusammenhänge misst und nicht-lineare Effekte systematisch unterschätzen würde, erfasst die Spearman-Korrelation jegliche monotonen Beziehungen robust und verteilungsunabhängig. 
Die resultierende Korrelationsmatrix ist in Abbildung \ref{fig:spearman_corr} als Heatmap visualisiert.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1\textwidth]{05_spearman.png}
    \caption[Spearman-Korrelationsmatrix]{Spearman-Korrelationsmatrix der Sensorwerte und der Zielvariable. Die Heatmap visualisiert die monotonen, nicht-linearen Abhängigkeiten zwischen den Features (Multikollinearität) sowie deren individuelle Vorhersagekraft für die Hebelstellung \texttt{M\_ATO\_RTBRq} (Feature Importance).}
    \label{fig:spearman_corr}
\end{figure}

Die Analyse der sogenannten Feature Importance, also der direkten Korrelation der Eingangsmerkmale mit der Zielvariable \texttt{M\_ATO\_RTBRq}, liefert zentrale Erkenntnisse für die Modellbildung. 
Den stärksten Zusammenhang weist das Signal \texttt{M\_RST\_TBsetVal} mit $\rho = 0{,}78$ auf. 
Da es sich hierbei jedoch um die fahrzeugseitige Rückmeldung der real gestellten Kraft handelt, repräsentiert dieser Wert eine reaktive Größe, die extrem nah am prädiktiven Ziel selbst liegt. 
\label{sec:leakage_mrst}
Die Verwendung dieses Features birgt ein hohes Risiko für Label-Leakage (das Modell erhält unbeabsichtigt die Lösung) und erfordert daher im weiteren Modellierungsverlauf höchste Vorsicht. 
Valide und signifikante prädiktive Zusammenhänge zeigen hingegen die geschätzte Beschleunigung \texttt{A\_EST} ($\rho = 0{,}56$), die Ist-Geschwindigkeit \texttt{V\_EST} ($\rho = 0{,}39$) sowie der Halteabstand \texttt{D\_STPDISTANCE} ($\rho = 0{,}38$), obgleich letzterer durch die zuvor beschriebene geringe Datenabdeckung von lediglich 38\,\% limitiert ist. 
Nur schwach korrelieren die zulässige Höchstgeschwindigkeit \texttt{V\_PERMITTED} ($\rho = 0{,}14$) und der Streckengradient \texttt{A\_GRADIENT} ($\rho = 0{,}09$). 
Die Variablen \texttt{M\_RST\_SlipSlide} ($\rho = -0{,}02$) und \texttt{TOPO\_Q\_RADIUS} ($\rho = 0{,}02$) erweisen sich hinsichtlich einer direkten monotonen Abhängigkeit als irrelevant für die Zielprädiktion.

Neben der Feature Importance offenbart die Matrix signifikante Interkorrelationen zwischen den unabhängigen Variablen, was auf eine ausgeprägte Multikollinearität hindeutet. 
Besonders hervorzuheben sind hierbei die starken Zusammenhänge zwischen \texttt{A\_EST} und \texttt{M\_RST\_TBsetVal} ($\rho = 0{,}68$) sowie innerhalb der kinematischen Metriken: \texttt{V\_EST} korreliert stark mit \texttt{V\_PERMITTED} ($\rho = 0{,}63$) und \texttt{D\_STPDISTANCE} ($\rho = 0{,}53$), während \texttt{V\_PERMITTED} und \texttt{D\_STPDISTANCE} ebenfalls eine deutliche Abhängigkeit aufweisen ($\rho = 0{,}46$).

Unter Aspekten der Modell-Generalisierung ist diese Multikollinearität äußerst kritisch zu bewerten. 
Redundante, hochkorrelierte Variablen blähen den Merkmalsraum unnötig auf und begünstigen den Fluch der Dimensionalität (Curse of Dimensionality). 
Sie liefern dem neuronalen Netz keine zusätzliche Informationstiefe, sondern verstärken stattdessen das zugrundeliegende Datenrauschen und erhöhen das Risiko für Overfitting massiv. 
Zudem erschweren sie die Attributionsstabilität des Modells, da der Optimierungsalgorithmus beim Gradientenabstieg willkürlich die Gewichte zwischen den redundanten Features verschieben kann, ohne den Fehler (Loss) zu verändern. 
Folglich bilden diese stark korrelierenden Sensordaten primäre Kandidaten für eine systematische Entfernung oder Aggregation im anstehenden \gls{FE}.

\subsubsection{Physikalischer Plausibilitätscheck}

Um die funktionale Integrität des bereinigten Datensatzes abschließend zu verifizieren, wurde eine übergeordnete Plausibilitätsprüfung (Sanity-Check) anhand aggregierter Kennzahlen durchgeführt. 
Der Abgleich der geschätzten Sensor-Beschleunigung \texttt{A\_EST} mit der numerischen Ableitung der Geschwindigkeit nach der Zeit ($\frac{dV}{dt}$) ergab eine hohe Spearman-Korrelation von $\rho = 0{,}81$ bei einem Skalierungsfaktor von etwa $6$, was die physikalische Konsistenz der Sensorik bestätigt. 
Der Faktor lässt sich technisch durch die kombinierte Einheiten- und Filtersystematik erklären: Während \texttt{A\_EST} \gls{SI}-basiert in \(\mathrm{mm/s^2}\) skaliert übertragen wird, liegt der Differenzenquotient aus \texttt{V\_EST} in \((\mathrm{km/h})/\mathrm{s}\) vor; allein die Umrechnung \(1\,\mathrm{m/s^2}=3{,}6\,(\mathrm{km/h})/\mathrm{s}\) erzeugt bereits einen systematischen Maßstabsversatz. 
Die darüber hinausgehende Abweichung resultiert aus der sensorseitigen Tiefpassfilterung und Glättung im Bordrechner, wodurch \texttt{A\_EST} gegenüber der roh differenzierten Geschwindigkeit amplituden- und phasenmodifiziert erscheint. 
Zudem stimmte das Vorzeichen der Hebelstellung in 78\,\% der Fälle mit der resultierenden Beschleunigungsrichtung überein. 
Auch die grundlegende Fahrlogik, wonach die Ist-Geschwindigkeit die zulässige Höchstgeschwindigkeit nicht überschreiten sollte (\texttt{V\_EST} $\le$ \texttt{V\_PERMITTED}), war in 90,7\,\% der Datenpunkte erfüllt. 
Physisch unmögliche Zustände wie negative Geschwindigkeiten (0\,\%) oder Werte über 150\,km/h traten nach der Bereinigung nicht mehr auf.

\begin{figure}[htbp]
    \centering
    \includegraphics[angle=90,height=0.9\textheight]{11_sample_trip.png}
    \caption[Zeitreihe einer Beispiel-Fahrt]{Zeitreihen-Analyse einer beispielhaften Fahrt. Die Darstellung überlagert die physikalischen Zustandsgrößen (Geschwindigkeit und Beschleunigung) mit den korrespondierenden Expertenaktionen (Hebelstellung) über die Zeit und verdeutlicht das physikalisch plausible Brems- und Beschleunigungsverhalten.}
    \label{fig:sample_trip}
\end{figure}

Ergänzend zu den aggregierten Metriken bestätigt die visuelle Analyse einer exemplarischen Fahrt (siehe Abbildung \ref{fig:sample_trip}) die Kausalität des Fahrerverhaltens im Streckenkontext. 
Es zeigt sich deutlich, dass Bremsphasen (Label $< 0$) systematisch mit einer sinkenden Geschwindigkeit sowie einem abnehmenden Halteabstand (\texttt{D\_STPDISTANCE}) zusammenfallen. 
Das Beschleunigungsverhalten ist gleichermaßen nachvollziehbar, da der Zugkraftaufbau primär dann initiiert wird, wenn eine ausreichende Geschwindigkeitsreserve (Differenz zwischen \texttt{V\_PERMITTED} und \texttt{V\_EST}) vorhanden ist.

Trotz dieser offensichtlichen physikalischen Plausibilität offenbart die Analyse signifikante Lücken im Merkmalsraum. 
Wesentliche physikalische und umweltbedingte Einflussgrößen (Hidden Variables) sind im Datensatz nicht repräsentiert. 
Dazu zählen fundamentale fahrzeugspezifische Konstanten wie die aktuelle Zugmasse beziehungsweise Beladung, die Zuglänge sowie die exakte Bremskraft-Charakteristik des eingesetzten Fahrzeugtyps. 
Auch Umwelteinflüsse wie Nässe, Laub, Temperatur oder Wind, welche die Adhäsion massiv beeinflussen, fehlen als direktes Messsignal; insbesondere ist das im Standardkontext vorgesehene Feld \texttt{Q\_ADHESION} im vorliegenden \gls{ORD}-Extrakt nicht belegt. 
Darüber hinaus fehlt der übergeordnete betriebliche Kontext, wie etwa Zeitdruck durch den Fahrplan, vorausschauende Signalbilder aus der \gls{LZB}, die über das aktuelle \texttt{V\_PERMITTED} hinausgehen, oder der spezifische Grund eines Halts.

Aus algorithmischer Sicht impliziert dieser Befund, dass das Imitationslernproblem nicht als vollständig beobachtbarer Markov-Entscheidungsprozess (\gls{MDP}), sondern als partiell beobachtbarer Prozess (\gls{POMDP}) aufzufassen ist: Der reale Systemzustand enthält latente Größen, die im Beobachtungsvektor nicht enthalten sind. 
Analoge Effekte der in Abschnitt 4.1 eingeführten \textit{Causal Confusion} zeigen sich hier bei der Adhäsionsdynamik: Wenn ein Triebfahrzeugführer aufgrund nasser Schienen frühzeitig bremst, entsprechende Reibwert- beziehungsweise Wettersignale aber fehlen, wird das Netz gezwungen, dieses Manöver beobachtbaren, jedoch ursächlich unbeteiligten Merkmalen zuzuschreiben. 
Methodisch muss die fehlende Reibwertinformation daher implizit über beobachtbare Proxys erschlossen werden, insbesondere über die dynamische Abweichung zwischen Soll- und Ist-Beschleunigung (\texttt{A\_EST}) in Kombination mit dem Radschlupf-Signal \texttt{M\_RST\_SlipSlide}. 
Ein konsistentes Lernziel besteht folglich darin, potenzielle Adhäsionsverluste nicht als direktes Eingangssignal zu erwarten, sondern als latenten Zustand aus diesen messbaren Fehlanpassungen zu inferieren.

\subsection{Feature Engineering}

Nach der initialen Bereinigung und Analyse der historischen Rohdaten bildet das domänenspezifische \gls{FE} den nächsten methodischen Schritt. 
Rohe Sensordaten allein bilden die zugrundeliegenden physikalischen Kausalitäten der Zugsteuerung oft nicht in einer Form ab, die von einem maschinellen Lernmodell effizient extrahiert werden kann. 
Um dem neuronalen Netz die komplexe Fahrentscheidung zu erleichtern und das Risiko von Kausalitätsfehlern zu minimieren, müssen die Eingangsdaten gezielt durch physikalisch motivierte, abgeleitete Merkmale ergänzt werden. 
Dieser Prozess transformiert implizites Systemwissen in explizite numerische Features, wodurch das Modell befähigt wird, vorausschauende statt rein reaktive Fahrstrategien zu erlernen.

\subsubsection{Methodik und Evaluierung}

Ein zentraler Aspekt bei der Konstruktion und Auswahl potenzieller Eingangsmerkmale ist die strikte Prävention von sogenanntem Label-Leakage. 
Wie die vorangegangene Korrelationsanalyse offenbarte, weist das rohe Feature \texttt{M\_RST\_TBsetVal} -- die fahrzeugseitige Rückmeldung der tatsächlich gestellten Kraft -- mit $\rho = 0{,}78$ die stärkste Spearman-Korrelation zur Zielvariable auf. 
Die detaillierte methodische Begründung für den Ausschluss dieses Signals wurde bereits in Abschnitt \ref{sec:leakage_mrst} hergeleitet; \texttt{M\_RST\_TBsetVal} wird daher im weiteren Verlauf konsequent nicht als Modelleingang verwendet.

Um eine konsistente zeitliche Basis für das Feature Engineering zu gewährleisten, erfolgt die Berechnung aller abgeleiteten Merkmale iterativ pro Fahrt (Trip) auf dem etablierten 200-ms-Raster. 
Für zeitliche Glättungen und Ableitungen werden dabei gleitende Fenster (Rolling Windows) von zehn Samples genutzt, was einem physikalischen Zeitraum von etwa zwei Sekunden entspricht.

Zur systematischen und objektiven Bewertung der Relevanz neu generierter Features wird eine Tri-Metrik-Evaluation angewendet. 
Da ein einzelnes statistisches Maß die komplexen Zusammenhänge in hochdimensionalen und multikollinearen Daten oft nur unzureichend oder verzerrt abbildet, werden drei komplementäre Evaluationsmetriken herangezogen:

Methodisch ist dabei eine klare Rollentrennung entscheidend: Die vier Roh-Sensorgrößen \texttt{V\_EST}, \texttt{V\_PERMITTED}, \texttt{A\_EST} und \texttt{A\_GRADIENT} definieren als \(S_{\text{base}} \in \mathbb{R}^4\) den physikalischen Minimal-Zustandsraum des Systems und werden domänengetrieben (nicht datengetrieben) gesetzt. 
Die Tri-Metrik-Evaluation dient ausschließlich der Selektion zusätzlicher, synthetisch konstruierter Kandidaten, um Informationsgewinn gegen Redundanz und Rauschanfälligkeit abzuwägen und den Zustandsraum nicht unnötig zu erweitern.

Insbesondere ist \texttt{A\_GRADIENT} trotz schwacher univariater Korrelation ($\rho = 0{,}09$) nicht verzichtbar. 
Der Streckengradient beschreibt mit \(F_{\text{st}} = m \cdot g \cdot \sin(\alpha)\) eine exogene Hangabtriebskraft und damit eine zur Kinematik orthogonale Erhaltungsgröße im Fahrwiderstands- und Leistungsmodell. 
Ohne diese Topologieinformation kann das Netz nicht kausal unterscheiden, ob ein Bremsmanöver durch Gefälle oder durch andere operative Randbedingungen induziert wurde; die Folge wären systematische Fehlattributionen in nichtlinearen Brems- und Zugkraftsituationen.

\begin{itemize}
    \item \textbf{Absoluter Spearman-Koeffizient ($|\rho|$):} Diese Metrik misst robuste, monotone -- und damit auch nicht-lineare -- Zusammenhänge zwischen dem jeweiligen potenziellen Feature und der Zielvariable.
    \item \textbf{\acrlongshort{MI}:} Dieses informationstheoretische Maß erfasst jegliche Form von Abhängigkeiten, einschließlich rein nicht-linearer und nicht-monotoner Informationsgehalte, die durch klassische Korrelationskoeffizienten nicht abgebildet werden.
    \item \textbf{Absoluter standardisierter \gls{OLS}-Koeffizient ($|\beta|$):} Mittels einer gewöhnlichen Fehlerquadratmethode (Ordinary Least Squares, \gls{OLS}) wird der tatsächliche multivariate Beitrag eines Features bewertet, wobei die Effekte bestehender Multikollinearität systematisch herausgerechnet werden.
\end{itemize}

Um Verzerrungen durch eine Einzelmetrik zu vermeiden, wird ergänzend ein aggregierter Gesamtindex als Composite Rank $R_{\text{composite}}$ gebildet. 
Die Berechnung erfolgt als arithmetisches Mittel der drei Einzelränge aus Spearman, \gls{MI} und \gls{OLS}:

\begin{equation}
    R_{\text{composite}} = \frac{R_{\text{Spearman}} + R_{\text{MI}} + R_{\text{OLS}}}{3}
\end{equation}

Da über drei diskrete Rangplätze gemittelt wird, entstehen systematisch gebrochene Rangwerte (z.\,B. $R_{\text{composite}} = 4{,}7$). 
Diese Dezimalränge bilden die relative Gesamtbedeutung eines Features im multidimensionalen Leistungsvergleich präziser ab als eine eindimensionale Ganzzahlordnung.

Durch die Kombination dieser drei Betrachtungswinkel lässt sich die Prädiktionsgüte jedes konstruierten Merkmals ganzheitlich quantifizieren, bevor es in den finalen Feature-Vektor des neuronalen Netzes aufgenommen wird.

\subsubsection{Kinematische Features}

Die erste Kategorie des \gls{FE} fokussiert sich auf die Bewegungsdynamik des Schienenfahrzeugs. 
Ziel ist es, aus den teils stark verrauschten rohen Geschwindigkeits- und Beschleunigungsdaten verlässliche Prädiktoren zu extrahieren, welche die tatsächliche Kinematik für das neuronale Netz optimal abbilden. 

Das aussagekräftigste Merkmal dieser Kategorie -- und gleichzeitig der Gesamtsieger der Evaluation (Rang 1) -- ist die zeitlich geglättete Beschleunigung \texttt{a\_est\_roll\_mean\_2s}. 
Sie berechnet sich als der gleitende Mittelwert (Simple Moving Average) des rohen Beschleunigungssignals über ein Fenster von zwei Sekunden ($n = 10$ Zeitschritte im 200-ms-Raster):

\begin{equation}
    a_{\text{est\_roll\_mean\_2s}, t} = \frac{1}{n} \sum_{i=0}^{n-1} a_{\text{est}, t-i} \quad \text{mit} \quad n=10
\end{equation}

Signaltheoretisch wirkt diese Operation wie ein maßgeblicher Tiefpassfilter. 
Das sprunghafte und von Sensorrauschen geprägte Originalsignal \texttt{A\_EST} wird entrauscht, wodurch der zugrundeliegende physikalische Trend maschinell verdaubar wird. 
Dies spiegelt sich in den hervorragenden Evaluationsmetriken wider: Das Feature erreicht einen Spearman-Koeffizienten von $0{,}52$ und die höchste \gls{MI} von $0{,}26$.

Als ergänzendes Feature wurde die numerische Ableitung der Ist-Geschwindigkeit nach der Zeit, \texttt{a\_num} ($\frac{dV}{dt}$), berechnet (Rang 3). 
Die Berechnung über das diskrete Zeitintervall $\Delta t = 0{,}2\,\text{s}$ lautet:

\begin{equation}
    a_{\text{num}, t} = \frac{v_{\text{est}, t} - v_{\text{est}, t-1}}{\Delta t}
\end{equation}

Obwohl dieses Merkmal eine starke monotone Beziehung zur Zielgröße aufweist (Spearman $0{,}43$), zeigt die ML-Bewertung Schwächen im multivariaten Kontext. 
Numerische Ableitungen realer Sensordaten sind prinzipbedingt ausreißerbehaftet. 
Zudem sinkt der standardisierte \gls{OLS}-Koeffizient auf $|\beta| = 0{,}15$, da \texttt{a\_num} stark kollinear (redundant) zur bereits vorhandenen Sensor-Beschleunigung \texttt{A\_EST} ist und dem Modell somit nur begrenzten multivariaten Mehrwert liefert.

Ein Paradebeispiel für die Notwendigkeit der Tri-Metrik-Evaluation liefert das Feature \texttt{v\_roll\_std\_2s} (Rang 6). 
Es beschreibt die gleitende Standardabweichung der Geschwindigkeit über ein Zwei-Sekunden-Fenster und quantifiziert die lokale Volatilität der Fahrt:

\begin{equation}
    v_{\text{roll\_std\_2s}, t} = \sqrt{\frac{1}{n-1} \sum_{i=0}^{n-1} \left( v_{\text{est}, t-i} - \bar{v}_{\text{est}, (t, n)} \right)^2} \quad \text{mit} \quad n=10
\end{equation}

Die lineare und monotone Korrelation dieses Features mit der Zielvariable ist praktisch null (Spearman $\approx 0$). 
Die \gls{MI} erreicht jedoch einen signifikanten Wert von $0{,}15$. 
Dieses Merkmal fungiert für das Modell als ein rein nicht-linearer Indikator für volatile Phasen, beispielsweise an den kritischen Übergängen zwischen Brems- und Beschleunigungsvorgängen, was von klassischen Korrelationsmetriken völlig unbemerkt bliebe.

Als am wenigsten geeignet (Rang 6,3) erwies sich hingegen der Ruck (\texttt{jerk}), welcher die zeitliche Änderungsrate der Beschleunigung darstellt:

\begin{equation}
    jerk_t = \frac{a_{\text{num}, t} - a_{\text{num}, t-1}}{\Delta t}
\end{equation}

Die physikalische und mathematische Natur dieses Features verdeutlicht eine fundamentale Grenze beim Feature Engineering mit realen Betriebsdaten. 
Als zweite numerische Ableitung des ohnehin leicht verrauschten Geschwindigkeitssignals degeneriert das Signal-Rausch-Verhältnis vollständig. Das resultierende Feature besteht fast ausschließlich aus hochfrequentem Rauschen, bietet dem neuronalen Netz keinerlei stabilen prädiktiven Mehrwert und verschlechtert potenziell die Robustheit der Policy.

\subsubsection{Kontext- und Regelabweichungs-Features}

Neben der reinen Bewegungsdynamik erfordert eine vorausschauende Fahrstrategie die situative Einordnung des Schienenfahrzeugs im betrieblichen Umfeld. 
Die nachfolgenden Merkmale quantifizieren die fortlaufende Regelabweichung, indem sie den aktuellen physikalischen Fahrzustand in Relation zu den deterministischen Streckenvorgaben -- insbesondere zu Geschwindigkeitsbegrenzungen und Haltepunkten -- setzen. 
Das neuronale Netz muss „wissen“, wie nah sich der Zug an seinen operationellen Limits und Zielen befindet.

Das effektivste Merkmal dieser Kategorie und das zweitbeste Feature der Gesamtauswertung (Rang 2) ist die relative Ausnutzung des zulässigen Geschwindigkeitslimits, definiert als \texttt{v\_ratio}:

\begin{equation}
    v_{\text{ratio}, t} = \frac{v_{\text{est}, t}}{v_{\text{permitted}, t}}
\end{equation}

Für die Modellierung erweist sich dieser Quotient als das mit Abstand beste Kontext-Feature. 
Durch die mathematische Normierung ist die Größe intrinsisch skaliert und gegenüber absoluten Geschwindigkeitsschwankungen äußerst robust. 
Dieser Informationsgehalt schlägt sich in einem extrem hohen multivariaten Beitrag nieder, welcher durch einen standardisierten \gls{OLS}-Koeffizienten von $|\beta| = 0{,}21$ quantifiziert wird. 
Das Modell lernt aus dieser Größe unmittelbar, wie viel Spielraum bis zum zwingenden Bremseingriff der Zugbeeinflussungssysteme verbleibt.

Ergänzend zur relativen Metrik wurde die absolute Geschwindigkeitsreserve \texttt{v\_headroom} berechnet (Rang 4,7):

\begin{equation}
    v_{\text{headroom}, t} = v_{\text{permitted}, t} - v_{\text{est}, t}
\end{equation}

Die Evaluation dieses Features veranschaulicht eindrucksvoll die Problematik der Multikollinearität im Feature Engineering. 
Univariat betrachtet ist das Merkmal äußerst aussagekräftig, was durch einen hohen \gls{MI}-Wert von $0{,}29$ belegt wird. 
Im multivariaten Kontext des Modells fällt die Relevanz jedoch signifikant ab ($|\beta| = 0{,}08$). 
Die Ursache hierfür liegt in der massiven Redundanz zu \texttt{v\_ratio} und den zugrundeliegenden rohen Geschwindigkeitssignalen. 
Das Merkmal bläht den Merkmalsraum auf, ohne dem Netz unabhängige, neue Informationen zu liefern.

Zur Modellierung des zielgenauen Bremsverhaltens vor Bahnhöfen oder Signalen wurde das Merkmal \texttt{stop\_proximity} entworfen (Rang 5). 
Um eine Singularität bei Erreichen des Zielpunktes ($D = 0$) zu vermeiden, wird die inverse Distanz unter Addition einer Konstanten berechnet:

\begin{equation}
    \text{stop\_proximity}_t = \frac{1}{|D_{\text{STPDISTANCE}, t}| + 1}
\end{equation}

ML-theoretisch und physikalisch stellt diese nicht-lineare Transformation einen brillanten Bremsindikator dar. 
Der Wert verharrt auf langen Streckenabschnitten nahe null und steigt exakt in der kritischen Phase kurz vor dem Haltepunkt exponentiell an, was dem Modell ein klares, starkes Signal für den Bremseinsatz liefert. 
Die praktische Nutzbarkeit in diesem spezifischen Anwendungsfall wird jedoch durch ein fundamentales Datenproblem stark limitiert: 
Wie die explorative Datenanalyse ergab, weist das zugrundeliegende Rohsignal \texttt{D\_STPDISTANCE} lediglich eine nutzbare Abdeckung (Coverage) von 38\,\% auf. 
Ein Modell, das bei der Prädiktion der Bremskurve ein hohes Gewicht auf dieses Feature legt, würde in den restlichen 62\,\% der Fälle mangels Sensordaten scheitern.

\subsubsection{Engineered Feature Pruning}

Vor der finalen Merkmalsreduktion wird der experimentelle Kreuzterm \texttt{grad\_x\_v} explizit eingeführt, der als Interaktion aus Streckengradient und Ist-Geschwindigkeit definiert ist:

\begin{equation}
    \texttt{grad\_x\_v}_t = A_{\text{GRADIENT}, t} \cdot V_{\text{EST}, t}
\end{equation}

Die physikalische Motivation dieses Terms liegt in der Abbildung eines neigungsabhängigen Lastanteils als Proxy für topologieinduzierten Fahrwiderstand beziehungsweise zusätzlichen Antriebsbedarf. 
Aus ML-Perspektive sollte das Produktmerkmal testen, ob eine explizite Interaktion zwischen Topologie und Fahrzustand zusätzliche nicht-lineare Information gegenüber den Einzelmerkmalen bereitstellt. 
Die Evaluation zeigt jedoch einen sehr geringen Informationsgehalt (Spearman $\rho = 0{,}06$, \gls{MI} $= 0{,}05$, standardisierter \gls{OLS}-Koeffizient $\beta = 0{,}06$, Rang 7). 
Ursächlich sind insbesondere die bereits schwache Grundkorrelation des Gradientsignals (Spearman $\rho = 0{,}09$) sowie die begrenzte Datenqualität durch Fehlwerte im Topologiekanal. 
Folglich erzeugt \texttt{grad\_x\_v} überwiegend zusätzliches Rauschen, erhöht die multiplikative Komplexität des Eingangsraums und liefert keinen belastbaren statistischen Mehrwert gegenüber den Einzel-Features; der Kreuzterm wird daher strikt aus dem finalen State Space ausgeschlossen.

Um die Erkenntnisse der Tri-Metrik-Evaluation zusammenzuführen und den finalen Eingangsvektor für das neuronale Netz zu definieren, werden die berechneten Wichtigkeiten in Abbildung \ref{fig:engineered_importance} visuell gegenübergestellt. 
Die Darstellung veranschaulicht eindrucksvoll die Notwendigkeit mehrerer Evaluierungsansätze: 
Insbesondere die Divergenz zwischen der Spearman-Korrelation und der \gls{MI} offenbart wertvolle, rein nicht-lineare Zusammenhänge, die von klassischen Korrelationsmaßen ignoriert werden, wie es signifikant bei der gleitenden Geschwindigkeitsstreuung (\texttt{v\_roll\_std\_2s}) der Fall ist.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1\textwidth]{06_engineered_importance.png}
    \caption[Vergleich der Feature-Wichtigkeit]{Vergleich der Feature-Wichtigkeit anhand dreier komplementärer Metriken. Die Divergenz zwischen Spearman-Korrelation und \gls{MI} offenbart rein nicht-lineare Zusammenhänge (z.\,B. bei \texttt{v\_roll\_std\_2s}).}
    \label{fig:engineered_importance}
\end{figure}

Basierend auf dieser quantitativen Datenlage erfolgt das abschließende Feature Pruning. 
In diesem Schritt werden gezielt Variablen aus dem Datensatz entfernt, die keinen unabhängigen Mehrwert liefern oder die numerische Stabilität des Trainings gefährden. 
Konsequent verworfen werden hochgradig redundante Merkmale wie die absolute Geschwindigkeitsreserve \texttt{v\_headroom}, deren Informationsgehalt bereits durch die robustere und normierte relative Ausnutzung \texttt{v\_ratio} abgedeckt ist. 
Ebenfalls ausgeschlossen wird die numerische Beschleunigungsableitung \texttt{a\_num} ($dV/dt$): Trotz hoher univariat-monotoner Relevanz ist dieses Feature im multivariaten Kontext stark kollinear zur direkt gemessenen Beschleunigung \texttt{A\_EST} beziehungsweise zu deren geglätteter Repräsentation \texttt{a\_est\_roll\_mean\_2s}. 
Damit liefert \texttt{a\_num} keinen substanziellen zusätzlichen Informationsgewinn, verstärkt jedoch aufgrund der numerischen Differentiation stochastisches Hochfrequenzrauschen und bläht den Eingangsraum unnötig auf. 
Neben \texttt{a\_num} werden auch stark verrauschte oder prädiktiv unbedeutende Variablen, darunter der Ruck (\texttt{jerk}) sowie weitere experimentelle Kreuzterme wie \texttt{grad\_x\_v}, aus dem finalen Merkmalsraum ausgeschlossen. 
Zugunsten eines orthogonalen, schlanken State Space werden folglich die physikalisch stabileren Beschleunigungsrepräsentationen \texttt{A\_EST} und \texttt{a\_est\_roll\_mean\_2s} priorisiert, während \texttt{a\_num} verworfen wird.

Für die Modellgüte ist dieser Reduktionsschritt entscheidend. 
Die bewusste Konzentration auf ein schlankes Modell mit weitestgehend orthogonalen, also mathematisch unabhängigen, Features verhindert effektiv den Fluch der Dimensionalität (Curse of Dimensionality). 
Ein kompakter, informationsdichter Zustandsraum entlastet den Optimierungsalgorithmus und reduziert die Gefahr irrelevanter Parameteranpassungen. 
Vor allem minimiert diese Strenge bei der Merkmalsauswahl das im Offline-Imitationslernen omnipräsente Risiko des Overfittings, da dem neuronalen Netz signifikant weniger Rauschquellen und Scheinkorrelationen zur fälschlichen Überanpassung (Memorization) zur Verfügung gestellt werden.

\subsection{Vorverarbeitung und Datensplitting}

Bevor der Datensatz dem neuronalen Netz zur Verarbeitung übergeben werden kann, müssen die bereinigten Rohdaten sowie die neu konstruierten physikalischen Merkmale in ein maschinenlesbares und numerisch stabiles Format überführt werden. 
Zudem ist eine methodisch stringente Aufteilung der Datenbasis konstitutiv, um das Modell im späteren Verlauf robust trainieren und objektiv sowie generalisierbar evaluieren zu können.

\subsubsection{Finale Feature-Auswahl und Label-Glättung}

Aufbauend auf der vorangegangenen Evaluierung definiert sich der finale Eingangsvektor (State Space) des Modells aus exakt acht Merkmalen. 
Diese setzen sich aus vier bereinigten Sensor-Rohdaten und vier abgeleiteten, domänenspezifischen Features zusammen. 
Variablen, die das Risiko von Data-Leakage bargen (insbesondere \texttt{M\_RST\_TBsetVal}, siehe Abschnitt \ref{sec:leakage_mrst}), multivariat redundant waren (wie \texttt{a\_num}) oder primär aus Rauschen bestanden (wie \texttt{jerk}), wurden endgültig aus dem Datensatz entfernt. 
Tabelle \ref{tab:final_features} bietet eine strukturierte Übersicht der finalen Modelleingänge.

\begin{table}[htbp]
    \centering
    \caption[Finaler Feature-Vektor]{Übersicht des finalen Feature-Vektors für das Modelltraining.}
    \label{tab:final_features}
    \renewcommand{\arraystretch}{1.2}
    \begin{tabular}{|l|l|l|}
        \hline
        \textbf{Kategorie} & \textbf{Feature-Name} & \textbf{Beschreibung / Typ} \\
        \hline
        \multirow{4}{*}{Roh-Sensorik} 
        & \texttt{V\_EST} & Ist-Geschwindigkeit des Zuges \\
        & \texttt{V\_PERMITTED} & Zulässige Höchstgeschwindigkeit (Streckenlimit) \\
        & \texttt{A\_EST} & Geschätzte physikalische Beschleunigung \\
        & \texttt{A\_GRADIENT} & Streckengradient (Topologie) \\
        \hline
        \multirow{4}{*}{Engineered} 
        & \texttt{v\_ratio} & Relative Ausnutzung des Limits ($V_{\text{EST}} / V_{\text{PERMITTED}}$) \\
        & \texttt{a\_est\_roll\_mean\_2s} & Geglättete Beschleunigung über 2 Sekunden \\
        & \texttt{v\_roll\_std\_2s} & Gleitende Streuung der Geschwindigkeit (Volatilität) \\
        & \texttt{stop\_proximity} & Transformierte Distanz zum nächsten Haltepunkt \\
        \hline
    \end{tabular}
\end{table}

Parallel zur Merkmalsselektion erfolgte eine temporale Glättung der Zielvariable (Label). 
Für das Training wurde die rohe Hebelstellung (\texttt{M\_ATO\_RTBRq}) über einen zentrierten gleitenden Mittelwert geglättet (fünf Samples, entsprechend einer Sekunde im 200-ms-Raster). 
Diese Label-Glättung ist methodisch von zentraler Bedeutung, um hochfrequentes, menschliches „Fahrer-Zittern“ am Hebel herauszufiltern. 
Das neuronale Netz soll in der Optimierung nicht dazu gezwungen werden, diese stochastischen Mikrobewegungen der menschlichen Hand zu imitieren, sondern die zugrundeliegende, makroskopische und physikalisch sinnvolle Brems- und Beschleunigungsentscheidung zu erlernen.

\subsubsection{Trip-basiertes Dataset-Splitting}

Um die Generalisierungsfähigkeit des Modells validieren zu können, wurde der Datensatz in drei disjunkte Mengen unterteilt. 
Das definierte Zielverhältnis bezogen auf die Gesamtfahrzeit lag bei 75\,\% für das Training, 15\,\% für die Validierung und 10\,\% für den finalen Test. 

Anstatt die Daten auf Zeilenebene aufzuteilen, wurde ein Trip-basierter \textit{Greedy Longest-First}-Algorithmus angewandt. 
Hierbei wurden alle Fahrten (Trips) zunächst absteigend nach ihrer Dauer sortiert. 
Anschließend wurde jede Fahrt iterativ als ungeteilter Block jenem Split (Train, Validation oder Test) zugewiesen, der prozentual zu diesem Zeitpunkt am weitesten von seinem definierten Zielwert entfernt war. 
Dieses Verfahren resultierte in einer hochpräzisen Aufteilung: Das Trainings-Set umfasst 208 Trips (109 Stunden), das Validierungs-Set 106 Trips (21,8 Stunden) und das Test-Set 89 Trips (14,5 Stunden).

Eine rechnerische Folge dieses Vorgehens ist jedoch eine systematische Asymmetrie in den mittleren Trip-Längen: 
Durch die priorisierte Zuweisung langer Fahrten in den jeweils am stärksten unterfüllten Ziel-Eimer entsteht im Trainings-Set eine deutlich höhere mittlere Fahrtdauer von etwa 31 Minuten pro Trip, während das Validierungs-Set im Mittel bei rund 12 Minuten und das Test-Set bei rund 10 Minuten liegt. 
Damit geht ein zusätzlicher Verteilungsunterschied einher: Das Training enthält überproportional viele eingeschwungene Beharrungsphasen längerer Streckenfahrten, wohingegen Validation und Test pro Zeiteinheit relativ mehr Anfahr-, Brems- und Übergangsprozesse abbilden. 
Aus ML-Sicht ist diese Asymmetrie kritisch zu dokumentieren, sie wirkt jedoch zugleich als bewusst harter Generalisierungstest: 
Das Modell muss zeigen, dass auf langen Sequenzen erlernte zeitliche Abhängigkeiten robust auf kürzere, dynamischere Fahrabschnitte übertragbar sind, anstatt lediglich monotone Reisefahrtmuster zu memorisieren.

Die ML-Begründung für dieses Vorgehen liegt in der strikten Vermeidung von temporalem Data-Leakage. 
Ein zufälliges Mischen (Random Shuffle) einzelner Zeilen über den gesamten Datensatz würde die Kausalität der Zeitreihen unwiderruflich zerstören. 
Das Netzwerk würde im Training zwangsläufig Samples aus der „Zukunft“ einer Fahrt sehen und könnte diese Informationen nutzen, um bei der Validierung künstlich zu glänzen. 
Der Trip-basierte Split garantiert stattdessen, dass die Validation- und Test-Sets zu 100\,\% aus ungesehenen, in sich geschlossenen und völlig neuen Fahrtsituationen bestehen.

\subsubsection{Skalierung und Imputation}

Im letzten Vorverarbeitungsschritt wurden sowohl die Eingangsfeatures als auch die Zielvariable skaliert, um eine einheitliche Werteverteilung für den Optimierungsalgorithmus zu gewährleisten und numerische Instabilitäten zu vermeiden.

Für die Eingangsdaten geschah dies in einem zweistufigen Prozess: 
Zunächst wurden extreme Ausreißer eliminiert, indem die Rohdaten auf das 0,5\,\%- und 99,5\,\%-Perzentil ($q_{\text{low}}$ und $q_{\text{high}}$) beschnitten wurden (Clipping):

\begin{equation}
    x_{\text{clipped}} = \min(\max(x, q_{\text{low}}), q_{\text{high}})
\end{equation}

Anschließend erfolgte eine Z-Score-Standardisierung. 
Die zuvor geclippten Merkmale wurden hierbei in einen Wertebereich transformiert, der einen Mittelwert von exakt $\mu = 0$ und eine Standardabweichung von $\sigma = 1$ aufweist:

\begin{equation}
    x_{\text{scaled}} = \frac{x_{\text{clipped}} - \mu_{\text{train}}}{\sigma_{\text{train}}}
\end{equation}

Die Reihenfolge und Methodik dieses Schrittes erfordert höchste methodische Disziplin zur Verhinderung von \textit{Scaling-Leakage}. 
Die Berechnung der Quantilsgrenzen ($q_{\text{low}}$, $q_{\text{high}}$) sowie von Mittelwert ($\mu_{\text{train}}$) und Standardabweichung ($\sigma_{\text{train}}$) wurde ausnahmslos und ausschließlich auf dem isolierten Trainingsdatensatz (Train-Split) durchgeführt. 
Diese statistischen Parameter wurden anschließend eingefroren, in einer \texttt{scaler.json}-Datei gespeichert und deterministisch auf das Validation- und Test-Set angewandt. 
Hätte man den Datensatz vor dem Splitting als Gesamtheit skaliert, wären statistische Verteilungsinformationen aus dem Test-Set implizit in das Training eingeflossen. 

Verbleibende \texttt{NaN}-Werte in den Eingangsfeatures, die beispielsweise durch Lückenfüller am Rande von Trips entstanden sind, wurden nach der Skalierung systematisch durch den Wert $0$ ersetzt. 
Diese Imputation birgt einen mathematisch eleganten ML-Vorteil: 
Da der Mittelwert jedes Features im Z-Raum durch die Standardisierung exakt $0$ beträgt, entspricht das Einsetzen der Null exakt dem Durchschnittswert des jeweiligen Features. 
Fehlt dem Modell somit punktuell eine Sensorinformation, erhält es den neutralsten aller möglichen Werte, wodurch das Netzwerk in seiner Prädiktion am wenigsten in die Irre geführt wird.

Für das Feature \texttt{stop\_proximity} besitzt diese Entscheidung besondere Relevanz, da dessen Datenabdeckung lediglich bei rund 38\,\% liegt und in etwa 62\,\% der Zeitschritte kein explizites Distanzsignal vorliegt. 
Die Nullimputation nach der Z-Score-Transformation ist in diesem Fall technisch konsistent, weil sie in Phasen ohne aktiven Haltepunkt den neutralen Erwartungswert einspeist und damit keine systematische Verzerrung der Gradientenupdates im Backpropagation-Prozess induziert. 
Gleichzeitig ist diese Lücke fachlich nicht als reiner Datenfehler zu interpretieren, sondern als Abbild des Betriebszustands auf freier Strecke ohne unmittelbar bevorstehenden \gls{ATO}-Halt. 
Kritisch bleibt jedoch, dass das Modell in diesen 62\,\% der Zeitpunkte Bremsentscheidungen nur aus sekundären Kinematik- und Limit-Signalen (insbesondere \texttt{V\_EST}, \texttt{V\_PERMITTED}, \texttt{A\_EST}) ableiten kann. 
Damit verschiebt sich die Annäherungslogik an Halte- oder Restriktionspunkte implizit von einem kontinuierlichen Distanzhinweis auf indirekte Veränderungen der zulässigen Geschwindigkeit \texttt{V\_PERMITTED}, was eine strukturelle Limitation des Zustandsraums transparent macht.

Analog zur Behandlung der Features wurde im selben Schritt auch die geglättete Zielvariable (Label) transformiert. 
Die Hebelstellung \texttt{M\_ATO\_RTBRq} wurde durch den systemspezifischen Maximalwert von $16384$ dividiert und anschließend strikt auf das zulässige Intervall $[-1, 1]$ geclippt. 
In dieser standardisierten Repräsentation definieren positive Werte eine Beschleunigungsanforderung, während negative Werte einen Bremsvorgang kodieren. 
Diese Skalierung fängt physikalisch unmögliche Spitzen ab und garantiert die numerische Stabilität der späteren Verlustfunktion:

\begin{equation}
    y_{\text{scaled}} = \min\left(\max\left(\frac{\text{M\_ATO\_RTBRq}_{\text{smoothed}}}{16384}, -1\right), 1\right)
\end{equation}